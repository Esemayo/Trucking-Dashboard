V1.5
we will now implement a 2 table system so we can now include fuel consumption per load and cost
loads 
-date
-load type
-load sequence 
-miles
-rate
calculated in loads
-rate per mile
-profit per mile

tomorrows objective write the schema for fuel table
fields:
table name:fuel_purchases
                -fuel_id INTEGER, required
                -purchase_date TEXT, required
                -gallons REAL, required
                -total_cost REAL, required
                -odometer REAL optional
                UNIQUE fuel id
how we track:
    -store reciepts after fueling up then do a block to store the data on loads and fuel also we write down odometer reading after fueling
validations:
    -gallons>0
    -total_cost>0
    odometer>=0 if present
    odometer missing insert null
    date- same format as loads table
metrics- were going to add price per gallon 
    price_per_gallon = total_cost / gallons
    and we willl be storing it to track changs over time

TOMMOROWS OBJECTIVE:
    -stregthen loads table by adding check null or not null to the db insert to defend against any bad data